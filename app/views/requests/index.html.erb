<p id="notice"><%= notice %></p>

<div class="page-header">
  <br>

<%= link_to 'Make a New Request', new_request_path, class: "btn btn-lg btn-primary"%>
<h3>Creating a request will create an email to both of us with all the required information.</h3>

<br><br><br>

Please check the availibity your requested date below.
<br><br><br>
<div class="row">
<div class="col-md-6"</div>
  

   <h1>Unprocessed Requests</h1>
<h3>I will be processing these requests once or twice a month. </h3>
 <table class="table table-striped">



  <thead>
    <tr>
      <th>Library</th>
      <th>Program</th>
      <th>Date</th>
      <th>Time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @requests.sort_by {|obj| obj.date}  %>    
    <% @requests.each do |request|  %>
      <tr>
        <% if request.confirmation != true %>
          <td><%= request.library %></td>
          <td><%= request.program %></td>
          <td><%= request.date %></td>
          <td><%= request.time %></td>
          <td><%= if request.created_at > 30.seconds.ago  then link_to 'Show', request end %></td>
          <td><%= if request.created_at > 30.seconds.ago  then link_to 'Edit', edit_request_path(request)  end %></td>
          <td><%= if request.created_at > 30.seconds.ago  then link_to 'Destroy', request, method: :delete, data: { confirm: 'Are you sure?' } end %></td>
       <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Request', new_request_path%>
 </div>
      <div class="col-md-6">
      
      <h1>Confirmed Dates</h1>
      <p>Confirmed programs are listed below.</p>
  <table class="table table-striped">     
      <thead>
    <tr>
      <th>Library</th>
      <th>Program</th>
      <th>Date</th>
      <th>Time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @requests.each do |request|  %>
      <tr>
        <% if request.confirmation == true %>
          <td><%= request.library %></td>
          <td><%= request.program %></td>
          <td><%= request.date %></td>
          <td><%= request.time %></td>
          <td><%= if request.created_at > 5.minutes.ago  then link_to 'Show', request end %></td>
          <td><%= if request.created_at > 5.minutes.ago  then link_to 'Edit', edit_request_path(request)  end %></td>
          <td><%= if request.created_at > 5.minutes.ago  then link_to 'Destroy', request, method: :delete, data: { confirm: 'Are you sure?' } end %></td>
       <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
  
      </div>

